apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fcg-usersapi
  namespace: fcg-prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /users
            pathType: Prefix
            backend:
              service:
                name: usersapi
                port:
                  number: 80
